name: hola mundo

on: 
  schedule:
    -  cron: "*/5 * * * *" #se ejecuta cada 5 minutos
  push:
    branches:
      - main

jobs:
  saludar:
    runs-on: ubuntu-latest
    steps:
      - name: Muestra mensaje hola mundo
        run: echo "hola mundo, Norman"
